<p id="notice"><%= notice %></p>

<h1><%= @page.name %></h1>

<%= link_to '新しいURLを追加', new_page_url_path, class:"btn btn-default btn-lg btn-block"  %>
<table class="table">
  <thead>
    <tr>
      <th>名前</th>
      <th>URL</th>
      <!-- <th>X</th>
      <th>Y</th>
      <th>W</th>
      <th>H</th>
      <th>Px</th>
      <th>Py</th>
      <th>Pw</th>
      <th>Ph</th>
      <th>Zoom</th>
      <th>Autoup</th>
      <th>Captime</th>
      <th>Hour</th>
      <th>Minute</th>
      <th>Visible</th>
      <th>User</th>
      <th>Page</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @urls.each do |url| %>
      <tr>
        <td><%= url.name %></td>
        <td><%= url.url %></td>
        <!-- <td><%= url.x %></td>
        <td><%= url.y %></td>
        <td><%= url.w %></td>
        <td><%= url.h %></td>
        <td><%= url.px %></td>
        <td><%= url.py %></td>
        <td><%= url.pw %></td>
        <td><%= url.ph %></td>
        <td><%= url.zoom %></td>
        <td><%= url.autoup %></td>
        <td><%= url.captime %></td>
        <td><%= url.hour %></td>
        <td><%= url.minute %></td>
        <td><%= url.visible %></td>
        <td><%= url.user_id %></td>
        <td><%= url.page_id %></td> -->
        <td><%= link_to '詳細', page_url_path(@page, url) %></td>
        <!-- <td><%= link_to '更新', capture_page_url_path(@page, url) %></td> -->
        <td><%= link_to '編集', edit_page_url_path(@page, url) %></td>
        <td><%= link_to '削除', page_url_path(@page, url), method: :delete, data: { confirm: '本当に削除しますか？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'ページ一覧に戻る', page_path(@page) %>
